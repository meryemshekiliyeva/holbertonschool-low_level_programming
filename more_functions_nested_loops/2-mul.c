#include "main.h"
#include <stdio.h>

/**
 * @a - integer
 * @b - integer
 * mul - Write a function that multiplies two integers.
 * Return: Always 0.
 */
int mul(int a, int b)
{
	printf("%d\n", a * b);
	return (0);
}
